import { EsqueletoDeConsole } from "@/components/ui/Esqueleto";

/**
 * A tela do evento não tem cabeçalho de página nem tiles: é um console. E é a
 * tela onde a espera dói mais — quem a abre está com o show começando.
 */
export default function Loading() {
  return <EsqueletoDeConsole />;
}
